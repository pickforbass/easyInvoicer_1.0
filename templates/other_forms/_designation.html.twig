{% use 'form_table_layout.html.twig' %}

{%- block form_row -%}
    {%- set widget_attr = {} -%}

    <td {% with {attr: row_attr} %} 
            {{ block('attributes')}} 
        {% endwith %}
    >
    {{- form_widget(form, widget_attr) -}}

{% endblock form_row %}



